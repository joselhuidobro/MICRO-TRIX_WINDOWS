# --- App ---
PORT=6800
SECRET_KEY=cambia-esta-clave

# --- Postgres local (del contenedor postgres_trix) ---
DB_HOST=postgres
DB_PORT=5432
DB_USER=trix
DB_PASSWORD=trixpass
DB_NAME=trixdb

# O fuerza explícitamente (opcional si tu get_database_uri ya arma desde DB_*):
DATABASE_URL=postgresql://trix:trixpass@postgres:5432/trixdb

# Cookies/sesión (mientras no uses HTTPS)
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_SECURE=0

