apiVersion: 1
providers:
  - name: "TRIX Dashboards"
    orgId: 1
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      foldersFromFilesStructure: true
      path: /var/lib/grafana/dashboards   # ← aquí buscará los .json

