apiVersion: 1
providers:
  - name: 'TRIX Dashboards'
    orgId: 1
    folder: 'TRIX'
    type: file
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      # Grafana montará tu carpeta local "grafana/dashboards" aquí si ya lo tienes en el compose

